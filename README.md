# 403-bypass-extension
Extensão de navegador (Chrome/Edge/Brave, Manifest V3). Ao encontrar um endpoint que retorna 403 Forbidden, ela lê o endpoint da aba ativa e dispara um conjunto de técnicas conhecidas de bypass, comparando cada resposta com o baseline.
